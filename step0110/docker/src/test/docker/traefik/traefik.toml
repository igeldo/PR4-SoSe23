################################################################
# Web configuration backend
################################################################
[web]
address = ":8080"

[file]

[frontends]
  [frontends.frontend1]
  backend = "idm"
    [frontends.frontend1.routes.idm]
    rule = "Host:idm,localhost"

# rules
[backends]
  [backends.idm]
    [backends.idm.servers.server1]
    url = "http://idm:8080"
